module Test.CLI.Parser exposing (tests)

import Expect
import Test exposing (Test, describe, test)
import CLI.Parser as Parser


dummyCmd =
    { word =
        "make"
    , args =
        Parser.ZeroOrMoreAargs Parser.grenFileParser
    , flags =
        Parser.initFlags 
            (\debug output ->
                { debug = debug
                , output = output
                }
            )
            |> Parser.onOff "debug" "debug desc"
            |> Parser.flag "output" Parser.pathParser "output path"
    , commonDescription =
        Just "make your project"
    , summary =
        "Makey make make"
    , example = "gren make src/Main.gren"
    , builder =
        \args flags ->
            Ok
                { word = "make"
                , args = args
                , flags = flags
                }
    }

tests : Test
tests =
    describe "CLI Parser"
        [ test "passing" <| \{} -> Expect.equal True False
        ] 
